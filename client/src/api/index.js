import { getPresignedURL } from './common'
import { addProject, fetchProjects, updateProjectOrder, deleteProject } from './projects'
import { fetchBlogApi, updateBlogOrder, deleteBlog } from './blogs'
export default { getPresignedURL, addProject, fetchProjects, fetchBlogApi, updateProjectOrder, updateBlogOrder, deleteBlog, deleteProject }
